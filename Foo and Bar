for num in range (100,1001):
    for w in range (2,num):
        if num % w == 0 and num % w != 0 :
            print num, "FooBar"
            break
        elif num % w == 0:
            print num, "Bar"
            break
    if num % w != 0:
        print num, "Foo"
            # break
